"""Reporting (Phase 9).

Produces the out-of-sample performance and walk-forward reports — Sharpe, max
drawdown, consistency — and, for the optimisation module, the explanation of
*why* a config won. Loud stub in Phase 1.
"""

from __future__ import annotations

from ..exceptions import stub


def build_report(results: object) -> object:
    """Render a validated performance/walk-forward report."""
    stub("reports.build_report", phase="Phase 9 (reporting)")
